[project]
name = "microver"
version = "0.1.0"
dependencies = [
    "pygame",
    "flask",
    "pyserial",
    "opencv-python",
    "waitress",
    "onnxruntime"
]

[project.scripts]
microver = "microver.main:main"
